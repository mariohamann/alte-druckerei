---
export interface Props {
	title: string;
}

const { title }: Props = Astro.props;
---

<h2 class='text-4xl lg:text-5xl font-bold text-white mt-32 mb-12'>{title}</h2>
<ul role='list' class='grid gap-12 grid-cols-1 md:grid-cols-2'>
	<slot />
</ul>
